import { login } from "./actions";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}) {
  const {error}=await searchParams;
  return <main className="login-shell"><section className="login-brand"><div className="seal">P</div><p>PRIDE LAW</p><h1>Run the firm with clarity.</h1><span>Secure case management for matters, clients, deadlines, documents, and billing.</span></section><section className="login-panel"><form action={login} className="login-form"><div className="eyebrow">FIRM OPERATIONS</div><h2>Welcome back</h2><p>Sign in with your authorized Pride Law account.</p>{error&&<div className="error">{error}</div>}<label>Email<input name="email" type="email" autoComplete="email" required /></label><label>Password<input name="password" type="password" autoComplete="current-password" required /></label><button className="primary" type="submit">Sign in</button><small>Protected by Supabase Auth and row-level security.</small></form></section></main>}
