import { createClient } from "@/lib/supabase/server";
const money=(n:number)=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(n);
export default async function Reports(){const s=await createClient();const [m,i,t,d]=await Promise.all([s.from("matters").select("practice_area,priority"),s.from("invoices").select("amount,balance,status"),s.from("tasks").select("status"),s.from("documents").select("id")]);const billed=(i.data||[]).reduce((n,x)=>n+Number(x.amount),0),collected=(i.data||[]).filter(x=>x.status==="Paid").reduce((n,x)=>n+Number(x.amount),0);return <section className="page"><div className="page-head"><div><span className="eyebrow">ANALYTICS</span><h1>Firm Reports</h1><p>Live operational and financial summaries.</p></div></div><div className="metrics"><Metric label="Total matters" value={String(m.data?.length||0)}/><Metric label="Billed" value={money(billed)}/><Metric label="Collected" value={money(collected)}/><Metric label="Documents" value={String(d.data?.length||0)}/></div><div className="dashboard-grid"><article className="card"><h2>Workload</h2><div className="list-row"><strong>Open tasks</strong><span>{(t.data||[]).filter(x=>x.status!=="Completed").length}</span></div><div className="list-row"><strong>High-priority matters</strong><span>{(m.data||[]).filter(x=>x.priority==="High").length}</span></div></article><article className="card"><h2>Financial</h2><div className="list-row"><strong>Outstanding</strong><span>{money((i.data||[]).reduce((n,x)=>n+Number(x.balance),0))}</span></div><div className="list-row"><strong>Collection rate</strong><span>{billed?Math.round(collected/billed*100):0}%</span></div></article></div></section>}
function Metric({label,value}:{label:string,value:string}){return <article className="card metric"><span>{label}</span><strong>{value}</strong><small>Live from PostgreSQL</small></article>}
